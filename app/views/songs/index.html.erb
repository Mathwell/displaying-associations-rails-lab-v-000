<h1>Songs</h1>
<table>
  <thead>
    <tr>
      <th>Artist Name</th>
      <th>Song Title</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @songs.each do |song| %>
      <tr>
        <td><%= song.artist_name %></td>
        <td><%= song.title %></td>
        <td>
          <%= link_to 'View', song %> |
          <%= link_to 'Edit', edit_song_path(song) %> |
          <%= link_to "Delete", song, method: :delete %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
